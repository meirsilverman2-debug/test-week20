# מבחן Backend — המערכה בצפון

## המערכה בצפון

במהלך יומיים עליכם לבנות שרת עבור משחק אסטרטגיה לשחקן יחיד מול המחשב. זהו תרחיש משחק דמיוני: השחקן מנהל את ישראל והמחשב מנהל את חיזבאללה. מטרת כל צד היא לכבוש את מפקדת היריב.

במבחן נבדקים תכנון בסיס הנתונים, טעינת מידע מקובץ, Express, API, ניהול מצב, בדיקות תקינות ואלגוריתמים. אין לבנות ממשק משתמש.

## הפרונט נתון מראש

אתם מקבלים פרויקט Frontend מוכן:

[https://github.com/hananFr/frontend-northern-war](https://github.com/hananFr/frontend-northern-war)

ניתן לראות כאן המחשה של המשחק המלא:

[https://northern-war.netlify.app/](https://northern-war.netlify.app/)

הפרונט הנתון הוא חלק מחוזה המבחן. עליכם לבנות את ה־API לפי הנתיבים, גופי הבקשות ומבני התשובות שמוגדרים במסמך זה, כך שלאחר חיבור כתובת השרת לפרונט יהיה אפשר לשחק במשחק מתחילתו ועד סופו.

* אין לשנות את מבנה הבקשות בפרונט כדי להתאים אותו לשרת שבניתם.  
* אין לשנות את מבנה התשובות שהוגדר במסמך.  
* מותר לעדכן בפרונט רק את כתובת השרת שאליה נשלחות הבקשות.  
* השרת חייב לאפשר CORS עבור הכתובת שממנה הפרונט רץ, לרבות Live Server בזמן הפיתוח.  
* יש לבדוק בפועל שאפשר לפתוח משחק, לבצע תור מלא, לרענן את הדף ולהמשיך את המשחק הקיים.  
* קוד הפרונט אינו חלק מההגשה ואינו מקבל ציון.

אתר ההמחשה נועד להציג את זרימת המשחק ואת התוצאה המצופה. המימוש שנבדק הוא השרת שלכם והיכולת שלו להפעיל את הפרונט שסופק.

## בחירת בסיס הנתונים

עליכם לבחור בעצמכם בסיס נתונים שמתאים לפרויקט ולחבר אותו לשרת. ניתן לבחור ב־Supabase/PostgreSQL, בסיס נתונים יחסי אחר או MongoDB.

לא ניתן מבנה טבלאות או collections מראש. עליכם להחליט:

* אילו נתונים נשמרים.  
* כיצד נשמרת המפה.  
* כיצד נשמר מצב של משחק פעיל.  
* כיצד מיוצגים מערכי שכנים ומצב הטריטוריות.  
* כיצד מונעים יצירת נתוני מפה כפולים בכל הפעלה של השרת.

ב־README יש להסביר בקצרה מדוע בסיס הנתונים שנבחר מתאים למבנה ולפעולות של המשחק. אין תשובה אחת נכונה, אך הנימוק צריך להתייחס לפרויקט ולא רק להיכרות או לנוחות אישית.

חובה לשמור את מצב המשחק בבסיס הנתונים. שמירה במשתנה או במערך בזיכרון השרת אינה מספיקה.

אינכם נבחנים על עדכון שדה פנימי בתוך אובייקט מקונן בבסיס הנתונים. לאחר פעולה חוקית מותר לקרוא את ישות ה־`game`, לעדכן אותה בקוד ולהחליף בבסיס הנתונים את כל הישות בגרסה המעודכנת. אין חובה להשתמש ב־nested update או לעדכן ישירות טריטוריה מסוימת בתוך המסמך השמור.

יש לשמור סודות ופרטי התחברות ב־`.env`. אין להעלות אותם ל־Git.

## קובץ המפה

תקבלו את `map.json`. כל איבר מתאר טריטוריה:

{

  "id": 10,

  "name": "נהריה",

  "x": 24,

  "y": 45,

  "neighbors": \[7, 13, 15\],

  "startOwner": "player",

  "headquarters": false,

  "distanceFromComputerHQ": 3,

  "distanceFromPlayerHQ": 3

}

* `neighbors` מכיל את מזהי הטריטוריות שמחוברות לטריטוריה.  
* `startOwner` הוא `player` או `computer`.  
* `headquarters` מסמן מפקדה. אם השדה חסר, ערכו `false`.  
* `x` הוא המרחק באחוזים מהקצה השמאלי של תמונת המפה.  
* `y` הוא המרחק באחוזים מהקצה העליון של תמונת המפה.  
* הנקודה `x, y` מייצגת את מרכז כרטיס הטריטוריה. כיוון ה־RTL של העמוד אינו משנה את משמעות הערכים.  
* יש לשמור את `x` ו־`y` כחלק מנתוני הטריטוריה.  
* `distanceFromComputerHQ` הוא מספר הצעדים הקבוע מהטריטוריה למפקדת המחשב.  
* `distanceFromPlayerHQ` הוא מספר הצעדים הקבוע מהטריטוריה למפקדת השחקן.

המרחקים כבר חושבו עבורכם. אין לחשב מסלולים או מרחקים בעצמכם.

בעת אתחול המערכת יש לבדוק אם נתוני המפה כבר קיימים בבסיס הנתונים. אם הם אינם קיימים, יש לקרוא את `map.json` ולשמור את כל הטריטוריות. אסור ליצור עותק נוסף של אותן טריטוריות בכל הפעלה.

יצירת משחק חדש תשתמש במפה שנקראה מבסיס הנתונים ולא תייבא מחדש את ה־JSON ישירות למצב המשחק.

## יצירת משחק

* כל טריטוריה רגילה מתחילה עם 4 חיילים.  
* כל מפקדה מתחילה עם 8 חיילים.  
* ביירות היא מפקדת המחשב.  
* ירושלים היא מפקדת השחקן.  
* השחקן מתחיל.  
* מספר הסיבוב מתחיל ב־1.  
* השלב הראשון הוא `reinforce`.  
* מצב המשחק הוא `playing` והמנצח הוא `null`.

### מבנה מצב המשחק

מצב משחק שנשמר בבסיס הנתונים ומוחזר מהשרת חייב לכלול לפחות:

{

  playerName: "Dana",

  round: 1,

  phase: "reinforce",

  status: "playing",

  winner: null,

  territories: \[\]

}

* `playerName` — שם השחקן שהתקבל ביצירת המשחק.  
* `round` — מספר הסיבוב הנוכחי.  
* `phase` — השלב הנוכחי בתור השחקן: `reinforce`, `attack` או `move`.  
* `status` — `playing` או `finished`.  
* `winner` — `player`, `computer` או `null` כל עוד המשחק לא הסתיים.  
* `territories` — המפה במצבה הנוכחי. כל טריטוריה כוללת את נתוני המפה המקוריים, ובנוסף `owner` ו־`soldiers`.

אין צורך בשדה `currentPlayer`. תור המחשב מתבצע במלואו בשרת לאחר `move` או `end-turn`, ולא נשמר כתור שממתין לפעולת משתמש.

מזהה המשחק נוצר על ידי בסיס הנתונים ומוחזר בתשובת ה־API. הוא אינו חייב להישמר פעם נוספת בתוך אובייקט מצב המשחק.

## סדר התור

תור השחקן:

reinforce → attack (תקיפה או דילוג) → move או end-turn

לאחר `move` או `end-turn`, המחשב מבצע תור מלא. בסיום תור המחשב:

* מגדילים את מספר הסיבוב ב־1.  
* מחזירים את השלב ל־`reinforce`.  
* מחזירים את השליטה לשחקן.

אסור לבצע פעולה שאינה שייכת לשלב הנוכחי.

### חיזוק

* השחקן בוחר טריטוריה אחת בבעלותו.  
* מוסיפים לו בדיוק 3 חיילים.  
* אי אפשר לפצל את החיזוק.  
* לאחר החיזוק עוברים ל־`attack`.

### תקיפה

השחקן רשאי לתקוף פעם אחת או לדלג.

תקיפה חוקית רק כאשר:

1. המקור שייך לשחקן.  
2. היעד שייך למחשב.  
3. היעד נמצא ב־`neighbors` של המקור.  
4. נשלח מספר שלם של חיילים.  
5. נשלח לפחות חייל אחד.  
6. נשאר לפחות חייל אחד במקור.

לאחר תקיפה או דילוג עוברים ל־`move`.

### העברת כוחות

השחקן רשאי להעביר כוחות פעם אחת או לדלג.

העברה חוקית רק כאשר:

1. המקור והיעד שייכים לשחקן.  
2. המקור והיעד שונים.  
3. נשלח מספר שלם של חיילים.  
4. נשלח לפחות חייל אחד.  
5. נשאר לפחות חייל אחד במקור.  
6. היעד נמצא ב־`neighbors` של המקור.

כל העברה היא לטריטוריה שכנה בלבד.

## אלגוריתם הקרב

attackLuck \= 0.6 \+ Math.random() \* 0.4;

defenseLuck \= 0.6 \+ Math.random() \* 0.4;

attackPower \= sentSoldiers \* attackLuck;

defensePower \= defendingSoldiers \* defenseLuck;

* התוקף מנצח רק כאשר `attackPower > defensePower`.  
* בכל מצב אחר, כולל שוויון, המגן מנצח.  
* החיילים שנשלחו מופחתים מהמקור בכל תוצאה.

אם התוקף ניצח:

survivors \= Math.max(

  1,

  Math.ceil(sentSoldiers \* (attackPower \- defensePower) / attackPower)

);

היעד עובר לבעלות התוקף ונשארים בו `survivors` חיילים.

אם המגן ניצח:

survivors \= Math.max(

  1,

  Math.ceil(defendingSoldiers \* (defensePower \- attackPower) / defensePower)

);

הבעלות אינה משתנה וביעד נשארים `survivors` חיילים.

כיבוש מפקדה מסיים את המשחק מיד. אין להמשיך את התור לאחר כיבוש מפקדה.

## אלגוריתם המחשב

תור המחשב:

חיזוק → תקיפה אפשרית → העברה אפשרית

אסור לבחור פעולות אקראיות. יש לממש את סדר העדיפויות הבא.

### הגדרות עזר

טריטוריית גבול היא טריטוריה של המחשב שיש לה לפחות טריטוריה שכנה אחת של השחקן.

### בחירת חיזוק

מוצאים את הערך הנמוך ביותר של `distanceFromComputerHQ` מבין הטריטוריות של השחקן.

אם המרחק הוא 2 או פחות, המחשב נמצא במצב הגנה. בוחרים טריטוריית גבול לפי:

1. הערך הנמוך ביותר של `distanceFromComputerHQ`.  
2. אם כמה טריטוריות נמצאות באותו מרחק — מספר החיילים הנמוך ביותר.  
3. אם עדיין יש שוויון — ה־`id` הנמוך ביותר.

אם המרחק גדול מ־2, המחשב נמצא במצב התקפה. בוחרים טריטוריית גבול לפי:

1. הערך הנמוך ביותר של `distanceFromPlayerHQ`.  
2. מספר החיילים הגבוה ביותר.  
3. ה־`id` הנמוך ביותר.

לטריטוריה שנבחרה מוסיפים בדיוק 3 חיילים.

### בחירת תקיפה

המחשב פועל לפי הכללים הבאים:

1. אם המחשב יכול לתקוף ישירות את מפקדת השחקן, ומספר החיילים שהוא יכול לשלוח גדול ממספר החיילים במפקדה, הוא מעדיף את התקיפה הזאת על פני כל תקיפה אחרת.  
2. נגד טריטוריה רגילה המחשב שוקל תקיפה רק כאשר מספר החיילים שהוא יכול לשלוח גדול לפחות ב־35% ממספר המגינים.  
3. מבין התקיפות האפשריות המחשב מעדיף תקיפה שמקדמת אותו לעבר מפקדת השחקן ושבה יתרון החיילים שלו גדול יותר.  
4. טריטוריה של השחקן שנמצאת קרוב למפקדת המחשב מקבלת עדיפות נוספת, כדי שהמחשב ינסה להסיר איום על מפקדתו.  
5. אם אין תקיפה שעומדת בתנאים, המחשב אינו חייב לתקוף וממשיך לשלב העברת הכוחות.

עבור כל מקור של המחשב ויעד שכן של השחקן:

// מספר החיילים שהמחשב יכול לשלוח תוך השארת חייל אחד בטריטוריית המקור.

sentSoldiers \= from.soldiers \- 1;

// היחס בין הכוח התוקף למספר החיילים שמגינים על טריטוריית היעד.

advantageRatio \= sentSoldiers / to.soldiers;

תקיפה נגד טריטוריה רגילה היא מועמדת רק כאשר:

advantageRatio \>= 1.35

אם היעד הוא מפקדת השחקן, התקיפה היא מועמדת כאשר:

sentSoldiers \> to.soldiers

לכל מועמדת לתקיפת המחשב:

// התקדמות: האם תקיפת היעד מקרבת את המחשב למפקדת השחקן.

// התוצאה היא 1 בהתקרבות, 0 ללא שינוי ו־1- בהתרחקות.

progress \= from.distanceFromPlayerHQ

  \- to.distanceFromPlayerHQ;

// יתרון החיילים: ככל שנשלחים יותר חיילים ביחס למגינים, הציון גבוה יותר.

soldierAdvantage \= sentSoldiers \- to.soldiers;

// הגנת המפקדה: תקיפת טריטוריה שמאיימת על מפקדת המחשב מקבלת בונוס.

protectsHeadquarters \= Math.max(

  0,

  3 \- to.distanceFromComputerHQ

) \* 25;

// התקדמות: תקיפה שמתקרבת למפקדת השחקן מקבלת משקל של 10 נקודות לצעד.

progressScore \= progress \* 10;

// כיבוש המפקדה הוא היעד החשוב ביותר ולכן מקבל בונוס גבוה במיוחד.

headquartersScore \= to.headquarters ? 1000 : 0;

// הציון הסופי מחבר את כל סדרי העדיפויות של המחשב.

score \= progressScore

  \+ soldierAdvantage

  \+ protectsHeadquarters

  \+ headquartersScore;

`progress` ו־`soldierAdvantage` מחוברים לאותו ציון: הראשון מעדיף התקדמות לעבר מפקדת השחקן, והשני מעדיף קרב שבו למחשב יתרון גדול יותר. הכפלה ב־10 נותנת עדיפות להתקדמות, בלי לבטל את השפעת יתרון החיילים.

הבונוס `protectsHeadquarters` נותן עדיפות לפעולה שמרחיקה איום ממפקדת המחשב. יעד שהוא מפקדת השחקן מקבל עדיפות עליונה.

המחשב מבצע את התקיפה בעלת הציון הגבוה ביותר ושולח את כל החיילים מלבד אחד.

בשוויון בוחרים יעד בעל `id` נמוך יותר, ולאחר מכן מקור בעל `id` נמוך יותר.

אם אין מועמדת שעוברת את סף `1.35`, המחשב אינו תוקף.

### העברת כוחות של המחשב

לאחר התקיפה המחשב בודק את כל זוגות הטריטוריות השכנות שבבעלותו. מקור חוקי חייב:

1. להיות בבעלות המחשב.  
2. להיות שונה מהיעד.  
3. להיות ללא שכן בבעלות השחקן.  
4. להכיל יותר מחייל אחד, או יותר מ־4 אם הוא מפקדה.

אם המחשב נמצא במצב הגנה, מעבר הוא מועמד רק כאשר:

to.distanceFromComputerHQ \< from.distanceFromComputerHQ

אם המחשב נמצא במצב התקפה, מעבר הוא מועמד רק כאשר:

to.distanceFromPlayerHQ \< from.distanceFromPlayerHQ

בוחרים מעבר לפי הסדר הבא:

1. המקור בעל מספר החיילים הגבוה ביותר.  
2. אם יש כמה מעברים שמקורם מכיל אותו מספר חיילים — בוחרים מביניהם את המעבר שהיעד שלו בעל ה־`id` הנמוך ביותר.  
3. אם לשני מעברים מועמדים יש גם אותו יעד — בוחרים את המקור בעל ה־`id` הנמוך ביותר.

מטריטוריה רגילה מעבירים את כל החיילים מלבד אחד. ממפקדה מעבירים את כל החיילים מלבד ארבעה. אם אין מקור חוקי, לא מתבצעת העברה.

## API נדרש

כל ה־endpoints מקבלים ומחזירים JSON, מלבד בקשה שאין לה גוף. מזהה המשחק בנתיב הוא המזהה שנוצר בבסיס הנתונים.

### מבנה תשובה של פעולה

ה־endpoints של חיזוק, תקיפה, העברה וסיום תור מחזירים:

{

  "game": {

    "id": "game-id",

    "playerName": "Dana",

    "round": 2,

    "phase": "reinforce",

    "status": "playing",

    "winner": null,

    "territories": \[\]

  },

  "playerEvent": null,

  "computerEvents": \[\]

}

* `game` הוא מצב המשחק המלא לאחר הפעולה.  
* `playerEvent` מתאר את הפעולה שביצע השחקן. בדילוג על תקיפה או בסיום תור ללא העברה ערכו `null`.  
* `computerEvents` הוא מערך הפעולות שביצע המחשב. לפני תור המחשב הוא ריק.

אירוע חיזוק כולל `type`,‏ `territoryId` ו־`soldiersAdded`. אירוע העברה כולל `type`,‏ `fromId`,‏ `toId` ו־`soldiers`. אירוע תקיפה כולל לפחות `type`,‏ `fromId`,‏ `toId`,‏ `soldiers` ו־`winner`.

### `POST /games` — יצירת משחק חדש

מטרת ה־endpoint היא ליצור משחק חדש ולהחזיר את מצבו ההתחלתי.

גוף הבקשה:

{ "playerName": "Dana" }

* `playerName` חייב להיות טקסט שאינו ריק. יש להסיר רווחים מיותרים מתחילתו ומסופו.  
* השרת קורא את המפה מבסיס הנתונים, יוצר עותק שלה עבור המשחק ומוסיף לכל טריטוריה `owner` ו־`soldiers`.  
* המשחק מתחיל בסיבוב 1, בשלב `reinforce` ובמצב `playing`.  
* השרת שומר את המשחק בבסיס הנתונים.

תגובה תקינה: `201 Created` ומצב המשחק המלא, כולל המזהה שנוצר:

{

  "id": "game-id",

  "playerName": "Dana",

  "round": 1,

  "phase": "reinforce",

  "status": "playing",

  "winner": null,

  "territories": \[\]

}

אם שם השחקן חסר או ריק, מחזירים `400 Bad Request` ולא יוצרים משחק.

### `GET /games/:id` — טעינת משחק

מטרת ה־endpoint היא לקבל משחק קיים מבסיס הנתונים, למשל לאחר רענון הדף.

* `:id` הוא מזהה המשחק שנוצר על ידי בסיס הנתונים.  
* אין גוף בקשה.  
* אין לשנות את מצב המשחק במהלך הבקשה.

תגובה תקינה: `200 OK` ומצב המשחק המלא באותו מבנה שמוחזר מיצירת משחק.

אם לא נמצא משחק בעל המזהה שנשלח, מחזירים:

{ "error": "המשחק לא נמצא" }

עם הסטטוס `404 Not Found`.

### `POST /games/:id/reinforce` — חיזוק טריטוריה

מטרת ה־endpoint היא לבצע את שלב החיזוק של השחקן.

גוף הבקשה:

{ "territoryId": 13 }

* המשחק חייב להיות פעיל ובשלב `reinforce`.  
* `territoryId` חייב לזהות טריטוריה בבעלות השחקן.  
* השרת מוסיף לטריטוריה בדיוק 3 חיילים ומשנה את השלב ל־`attack`.  
* לאחר הצלחה השרת שומר את מצב המשחק המעודכן.

תגובה תקינה: `200 OK` במבנה תשובת הפעולה. `playerEvent` מתאר את החיזוק ו־`computerEvents` הוא מערך ריק, משום שתור המחשב עדיין לא התחיל.

### `POST /games/:id/attack` — תקיפה או דילוג על תקיפה

מטרת ה־endpoint היא לבצע את שלב התקיפה היחיד של השחקן או לדלג עליו.

כדי לתקוף שולחים:

{ "fromId": 10, "toId": 7, "soldiers": 5 }

* `fromId` הוא מזהה טריטוריית המקור של השחקן.  
* `toId` הוא מזהה טריטוריית יעד שכנה בבעלות המחשב.  
* `soldiers` הוא מספר החיילים שנשלחים לקרב.  
* המשחק חייב להיות פעיל ובשלב `attack`.  
* השרת בודק בעלות, שכנות, מספר חיילים שלם וחיובי והשארת חייל אחד לפחות במקור.  
* השרת מפעיל את אלגוריתם הקרב. אם נכבשה מפקדה, המשחק מסתיים מיד. אחרת השלב משתנה ל־`move`.

כדי לדלג על התקיפה שולחים לאותו endpoint:

{ "skip": true }

דילוג מותר רק בשלב `attack`. במקרה זה לא מתבצע קרב, `playerEvent` הוא `null` והשלב משתנה ל־`move`.

תגובה תקינה: `200 OK` במבנה תשובת הפעולה. בתקיפה `playerEvent` מתאר את הקרב; בדילוג ערכו `null`. בשני המקרים `computerEvents` עדיין ריק.

### `POST /games/:id/move` — העברת כוחות וסיום תור השחקן

מטרת ה־endpoint היא לבצע העברת כוחות אחת ולסיים את תור השחקן.

גוף הבקשה:

{ "fromId": 17, "toId": 14, "soldiers": 3 }

* `fromId` ו־`toId` חייבים לזהות שתי טריטוריות שכנות ושונות בבעלות השחקן.  
* `soldiers` חייב להיות מספר שלם וחיובי, ולאחר ההעברה חייב להישאר לפחות חייל אחד במקור.  
* המשחק חייב להיות פעיל ובשלב `move`.  
* לאחר ההעברה השרת מפעיל תור מחשב מלא: חיזוק, תקיפה אפשרית והעברה אפשרית.  
* אם המשחק לא הסתיים, השרת מגדיל את מספר הסיבוב ומחזיר את השלב ל־`reinforce`.

תגובה תקינה: `200 OK` במבנה תשובת הפעולה. `playerEvent` מתאר את העברת השחקן ו־`computerEvents` מכיל את הפעולות שביצע המחשב לפי סדר ביצוען.

### `POST /games/:id/end-turn` — סיום תור ללא העברה

מטרת ה־endpoint היא לאפשר לשחקן לוותר על העברת כוחות ולסיים את תורו.

* אין גוף בקשה.  
* המשחק חייב להיות פעיל ובשלב `move`.  
* לא מתבצעת פעולת שחקן נוספת.  
* השרת מפעיל תור מחשב מלא.  
* אם המשחק לא הסתיים, מספר הסיבוב גדל והשלב חוזר ל־`reinforce`.

תגובה תקינה: `200 OK` במבנה תשובת הפעולה. `playerEvent` הוא `null` ו־`computerEvents` מכיל את פעולות המחשב.

### שגיאות ושמירת מצב

* משחק שלא נמצא מחזיר `404 Not Found`.  
* משחק שכבר הסתיים מחזיר `409 Conflict` בכל ניסיון לבצע פעולה נוספת.  
* שלב שגוי, טריטוריה לא חוקית או מספר חיילים לא חוקי מחזירים `400 Bad Request` עם `{ "error": "הסבר ברור" }`.  
* שגיאה בלתי צפויה מחזירה `500 Internal Server Error`.  
* בדיקות התקינות, הקרב ותור המחשב מתבצעים בשרת בלבד.  
* לאחר פעולה חוקית יש לשמור את המצב המעודכן בבסיס הנתונים. לאחר פעולה לא חוקית אין לשנות את המצב השמור.

שמירת המצב המעודכן יכולה להתבצע באמצעות החלפה מלאה של ישות ה־`game`. החלפה מלאה נחשבת מימוש תקין ואינה גוררת הפחתת נקודות.

## בדיקות חובה

יש לכתוב בדיקות ללוגיקת המשחק בלי להתחבר לבסיס נתונים אמיתי.

יש לבדוק לפחות:

1. **הגנה על מפקדת המחשב:** משנים את צידון, שמזהה ה־`id` שלה הוא 4, לבעלות השחקן ונותנים לה 10 חיילים. לאחר הפעלת תור המחשב, פעולת החיזוק הראשונה צריכה להוסיף 3 חיילים לביירות, שמזהה ה־`id` שלה הוא 2\.  
2. **כיבוש ישיר של מפקדת השחקן:** משנים את עפולה, שמזהה ה־`id` שלה הוא 16, לבעלות המחשב ונותנים לה 20 חיילים. בירושלים משאירים 3 חיילים. לאחר הפעלת תור המחשב, יעד התקיפה צריך להיות ירושלים, שמזהה ה־`id` שלה הוא 17, והמשחק צריך להסתיים בניצחון המחשב.  
3. **תור ללא תקיפה:** נותנים 20 חיילים לכל טריטוריה בבעלות השחקן. לאחר הפעלת תור המחשב, צריכה להופיע פעולת חיזוק אך לא צריכה להופיע פעולת תקיפה.  
4. **העברה מטריטוריה עורפית בלבד:** במצב הפתיחה המחשב אמור לבצע העברה. לפני הפעלת תור המחשב שומרים את מזהי כל הטריטוריות של המחשב שאין להן טריטוריה שכנה בבעלות השחקן. לאחר התור מוודאים שקיימת פעולת העברה ושמזהה טריטוריית המקור שלה מופיע ברשימה שנשמרה.

## הגשה בסוף המבחן

* שרת Express עובד.  
* חיבור לבסיס הנתונים שבחרתם.  
* `.env.example` ללא סודות.  
* טעינת `map.json` לבסיס הנתונים.  
* כל ה־endpoints הנדרשים.  
* בדיקות אוטומטיות.  
* קובץ README עם הוראות התקנה והרצה.

אין להגיש ממשק משתמש. יש להגיש את צד השרת בלבד ולוודא שהוא עובד מול הפרונט שסופק.  
